interface:
  display_name: "Mergepay Web"
  short_description: "Repo-specific patterns and workflows for mergepay-web"
  default_prompt: "Use the mergepay-web repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true